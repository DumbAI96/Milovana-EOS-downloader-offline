Drop browser-fetched files here (inbox).

The downloader will ask you to save the tease script from your browser as:
    script-<teaseId>.json
into this folder. It picks it up from here automatically.
